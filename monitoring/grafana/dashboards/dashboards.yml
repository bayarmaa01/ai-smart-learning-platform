apiVersion: 1

providers:
  - name: EduAI Dashboards
    orgId: 1
    folder: EduAI
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
